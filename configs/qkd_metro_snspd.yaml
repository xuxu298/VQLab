# Configured device (DeviceSpec): the SNSPD upgrade path — same protocol/optics, swapped
# detector. Flip `detector` to snspd and the sim, BOM, board parameters and rules all update.
name: qkd_metro_snspd
protocol: decoy_bb84
detector: snspd
n_channels: 2
gate_rate_hz: 1.25e9
distance_km: 50.0
bob_insertion_db: 3.5
e_misalignment: 0.01
n_z_block: 1.0e8
notes: "Upgrade: higher PDE + reach at far higher cost (cryostat). Same Alice, same fiber."
